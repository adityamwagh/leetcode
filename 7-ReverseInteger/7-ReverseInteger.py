class Solution:
    def reverse(self, x: int) -> int:
        isNeg = -1 if x < 0 else 1
        snum, rnum = str(x), ""
        for i in range(len(snum)-1, -1, -1):
            if snum[i] != "-":
                rnum += snum[i]
        
        return 0
        
        if -1 * 2**31 <= n <= 2**31 - 1:
        n = int(rnum) * isNeg
            return n

1
